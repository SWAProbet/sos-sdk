{
  "name": "@swa/uof-sdk",
  "version": "1.0.0",
  "description": "SWA Unified Odds Feed client SDK: connects to the SWA UOF Server over AMQP and consumes live odds",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12"
  },
  "files": [
    "dist/**/*"
  ]
}
